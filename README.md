# beck

Local skills router CLI for AI agents. Indexes SKILL.md files on disk and
serves the right one on demand, so agents stop burning tokens on skill
metadata in their system prompts.

This is the `0.0.1` name reservation. The real release is `0.1.0`, tracked
in `HANDOFF.md`.
